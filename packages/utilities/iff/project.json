{
  "name": "iff",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/utilities/iff",
  "projectType": "library",
  "targets": {
    "test": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "command": "cd packages/utilities/iff && python -m pytest",
        "cwd": "packages/utilities/iff"
      }
    },
    "lint": {
      "executor": "@nrwl/workspace:run-commands",
      "options": {
        "command": "cd packages/utilities/iff && python -m ruff check .",
        "cwd": "packages/utilities/iff"
      }
    }
  },
  "tags": [
    "scope:utilities",
    "type:utility"
  ]
}
